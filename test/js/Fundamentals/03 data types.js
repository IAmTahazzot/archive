/**
 * @data_types
 *
 * JavaScript has some primitive data types such as:
 *
 * @string
 * @number
 * @boolean
 * @null
 * @undefined
 * @symbol
 *
 * Types are dynamic which means it determine automatically by JS engine,
 * which is also called type coercion.
 */

const my_name = 'Tahazzot', // string
    age = 20, // number
    isMarried = false, // boolean
    monthlySalary = null, // null (not empty but it means no value)
    personality = undefined; // undefined
