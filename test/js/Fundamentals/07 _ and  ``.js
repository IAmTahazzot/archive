/*
String is a sequence of characters and template literal is a special kind of
string where can evaluate code inside it.

'this is a string'
`this is a template literal, today is ${new Date()}`

string has many build in methods.
 */

// + additional operator use to add numerical value and concatenation of string
const str = 'I am ' + 'Tahazzot';

// literals
const literals = `Hey, do you know it's ${new Date().getFullYear()}`