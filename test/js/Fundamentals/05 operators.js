/**
 *
 * There are several operators in javascript such as
 *
 *
 * =, !=, +=, -=, /=, *=, +, -, /, *, %, <, >, >=, <= etc...
 */

let x = 20,
    y = 30;

x++; // will increase by 1
--y; // will decrease by 1
