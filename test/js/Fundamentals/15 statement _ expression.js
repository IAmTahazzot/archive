// expression is a piece of code that evaluates values.
// statement doesn't evaluates values rather it perform a specific task.

true;
false;
10;
2 ** 9 % 3;
// all the above are expression that evaluates values or just a value;

// If else is a statement that perform a specific task
if (true) {
    console.log('Success');
}

// Only expression are allowed in string template literals
const name = 'Tahazzot';
`I am ${name}`;