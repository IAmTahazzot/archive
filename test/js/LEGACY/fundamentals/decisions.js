// It's all about if else statement the conditional operators
const age = 17;

if (age >= 18) {
  console.log('You are an Adult, Congrats');
} else {
  const timeLeft = 18 - age;
  console.log(`You have to wait ${timeLeft} Years`);
}
